language: java
jdk:
  - oraclejdk7
  - oraclejdk8
  - openjdk6
  - openjdk7
after_success:
  # Coberturaにする場合は以下を有効にする
  - mvn clean cobertura:cobertura coveralls:cobertura
  # Jacocoにする場合は以下を有効にする
  #- mvn clean test jacoco:report coveralls:jacoco
